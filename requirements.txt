# Core (offline-runnable, no network needed)
numpy>=1.24
scikit-learn>=1.3
faiss-cpu>=1.7.4

# Real dataset + production embedder (network needed on first run only,
# to pull ai4bharat/MSMARCO-XI and the multilingual-e5-small weights)
datasets>=2.19
sentence-transformers>=2.7
huggingface_hub>=0.23
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
openai>=1.14.0
httpx>=0.27.0
numpy>=1.26.0
tabulate>=0.9.0